import Layout from '@/layouts'

export const asyncRouterMap = [
  {
    path: '/',
    name: 'index',
    meta: { title: '首页' },
    component: Layout
  }
]

// 基础路由
export const constantRouterMap = []
